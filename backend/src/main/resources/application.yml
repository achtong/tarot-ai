spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tarot?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: tarot
    password: tarot
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
