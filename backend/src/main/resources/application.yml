spring:
  profiles:
    active: local
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY:}
        chat:
          options:
            model: gemini-3.6-flash
  datasource:
    url: jdbc:mysql://localhost:3306/tarot?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: tarot
    password: tarot
    driver-class-name: com.mysql.cj.jdbc.Driver
  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    consumer:
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "com.tarot.demo.DTO"
        spring.json.value.default.type: "com.tarot.demo.DTO.CouponIssueMessage"
        spring.json.use.type.headers: false
  data:
    redis:
      host: localhost
      port: 6379
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
